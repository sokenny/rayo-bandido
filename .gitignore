node_modules
dist
.vite
*.log
.DS_Store
artifacts

# The RAYO RUSH board's local store (`server/leaderboard.mjs`). A running server's scores are
# its own; `RB_DATA_DIR` moves them somewhere that survives a deploy.
.data

# Elastic Beanstalk Files
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml
